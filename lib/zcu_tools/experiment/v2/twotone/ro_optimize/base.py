import numpy as np


def signal2snr(signals: np.ndarray) -> np.ndarray:
    pass
    # if axis == 0:
    #     return np.abs(signals[0, :] - signals[1, :])
    # else:
    #     return np.abs(signals[:, 0] - signals[:, 1])
