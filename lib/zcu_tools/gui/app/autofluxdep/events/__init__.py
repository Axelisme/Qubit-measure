"""Autofluxdep-gui domain event definitions (workflow + run).

Import from the domain modules directly:
  - ``zcu_tools.gui.app.autofluxdep.events.workflow`` — workflow editing events
  - ``zcu_tools.gui.app.autofluxdep.events.run`` — run lifecycle events
"""
