"""fluxdep-gui domain services — thin wrappers over the headless analysis
functions (reused from ``zcu_tools.notebook.analysis.fluxdep`` and
``zcu_tools.utils``) that mutate ``FluxDepState`` on the Qt main thread.
"""
