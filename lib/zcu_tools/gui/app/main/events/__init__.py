"""Measure-gui domain event definitions (tab + run).

Import from the domain modules directly:
  - ``zcu_tools.gui.app.main.events.tab`` — tab lifecycle events
  - ``zcu_tools.gui.app.main.events.run`` — run lifecycle events
"""
